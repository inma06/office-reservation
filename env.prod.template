# Production Environment Variables Template
# AWS EC2 배포 시 .env.prod 파일을 생성하여 실제 값으로 채워주세요
# 사용법: cp env.prod.template .env.prod

# ============================================
# Database Configuration
# ============================================
# AWS RDS를 사용하는 경우 RDS 엔드포인트를 입력하세요
# 예: your-db-instance.xxxxx.ap-northeast-2.rds.amazonaws.com
DB_HOST=your-rds-endpoint.ap-northeast-2.rds.amazonaws.com
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your-secure-password-here
DB_DATABASE=reservation_db

# ============================================
# Application Configuration
# ============================================
PORT=3000
NODE_ENV=production

# ============================================
# JWT Configuration
# ============================================
# 운영 환경에서는 반드시 강력한 랜덤 문자열로 변경하세요!
# 생성 방법 (터미널에서 실행):
#   openssl rand -base64 32
# 또는:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# 
# ⚠️ 보안: 이 값이 노출되면 모든 JWT 토큰이 위조될 수 있습니다!
# 최소 32자 이상의 예측 불가능한 랜덤 문자열을 사용하세요.
JWT_SECRET=your-jwt-secret-key-here-CHANGE-THIS-IN-PRODUCTION

# ============================================
# CORS Configuration
# ============================================
# 프론트엔드 도메인 또는 IP 주소 (쉼표로 구분하여 여러 개 지정 가능)
# HTTPS 사용 시 https://your-domain.com 형식으로 지정
# 예: https://your-domain.com,https://www.your-domain.com
CORS_ORIGIN=https://dev-leo.site

# ============================================
# API Base URL (프론트엔드 빌드 시 사용)
# ============================================
# 백엔드 API의 전체 URL
# HTTPS 사용 시 https://your-domain.com/api 형식으로 지정
# 예: https://your-domain.com/api
API_BASE_URL=https://dev-leo.site/api

# ============================================
# Domain Configuration (HTTPS/SSL)
# ============================================
# Let's Encrypt 인증서 발급을 위한 도메인
# 예: example.com 또는 subdomain.example.com
DOMAIN=dev-leo.site

# ============================================
# Google OAuth (선택사항)
# ============================================
# Google 로그인을 사용하지 않는 경우 비워두세요
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Google OAuth 콜백 URL
GOOGLE_CALLBACK_URL=https://dev-leo.site/auth/google/callback
